{
  "name": "gazon59-root",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "install-all": "npm install --prefix backend && npm install --prefix fullstack/client && npm install --prefix fullstack/admin",
    "dev:backend": "npm run dev --prefix backend",
    "dev:client": "npm run dev --prefix fullstack/client",
    "dev:admin": "npm run dev --prefix fullstack/admin",
    "dev": "npx concurrently \"npm run dev:backend\" \"npm run dev:client\" \"npm run dev:admin\""
  },
  "devDependencies": {
    "concurrently": "^8.2.0"
  }
}
